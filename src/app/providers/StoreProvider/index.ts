import { StoreProvider } from './ui/StoreProvider';
import { createReduxStore, defaultState } from './config/store';

import { type StateScheme } from './config/StateScheme'

export {
  StoreProvider,
  createReduxStore,
  defaultState,
  type StateScheme
}
